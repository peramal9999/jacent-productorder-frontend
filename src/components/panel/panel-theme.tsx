import Image from "@/components/shared/image";import Link from "next/link";import {themes} from "@/components/panel/data";export function PanelTheme() {    return (        <div className="bg-white dark:bg-gray-200/80 rounded-lg p-5 shadow-sm">            <h3 className="text-base font-medium mb-4">Theme Selection</h3>            <div className="grid grid-cols-2 gap-5">                {themes.map((theme, index) => (                    <Link                        key={index}                        href={{pathname:theme.href }}                        className="cursor-pointer  relative p-1.5 bg-white rounded-md overflow-hidden text-center drop-shadow-dropDown border border-border-base hover:border-brand-dark transition-all duration-300 "                    >                            <Image                                src={theme.img }                                width={180}                                height={200}                                alt={theme.name || 'Product Image'}                                className="object-cover bg-fill-thumbnail"                            />                                                <p className="text-13px text-brand-dark font-medium pt-3 pb-2">{theme.name}</p>                    </Link>                ))}            </div>        </div>    )}